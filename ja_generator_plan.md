# 日本語指示文 生成計画（プリミティブ組み合わせ方式）

## 背景・方針

意味AST（`semantic_ast/schema.py`）は40,589種類ある。これ1件ごとにQwen3-4B-AWQへ「この意味ASTを説明する日本語文を書いて」と投げてチェックするのは、

- 生成コストが40,589回分かかる
- 人間チェックも40,589件必要になる
- しかも意味ASTは4つのカテゴリ（filter/map/order/slice）の**組み合わせ**でしかなく、日本語表現が変化するのは各カテゴリ内の**原子操作**の言い方だけである

という点で無駄が大きい。そこで `homework.md` の「日本語指示の生成」節が推奨する二段階方式（Qwen3にアトミックな操作の表現を10〜30種類生成させる→人間が承認→ルールベース生成器が組み合わせる）を、schema.pyの語彙に対して具体化する。

Qwen3に生成させる対象は**原子操作の語彙**（下記「1. プリミティブ要素」で列挙する高々20種強）だけであり、40,589種の意味AST自体は生成しない。意味AST→日本語文は、承認済み表現辞書からのサンプリングとテンプレート結合という**決定的な処理**にする。

```
schema.py の原子操作語彙 (24プリミティブ + 文頭/文末フレーム)
        │  Qwen3-4B-AWQ に「この操作の言い方を10〜30種類」を1プリミティブずつ問い合わせ
        ▼
候補表現（プリミティブごとに10〜30件）
        │  人間がチェック（意味が変わっていないか。例:「以上」→「より大きい」の混入を除外）
        ▼
承認済み表現辞書 (expressions/*.json 想定)
        │  ルールベース文生成器（本ドキュメントの組み合わせ型）
        ▼
意味AST 1件 → 複数の instruction_ja 候補（テンプレート×表現辞書の直積から抽選）
        ▼
instruction_ja
```

---

## 1. プリミティブ要素

`schema.py` の語彙をそのままプリミティブ単位とする。各プリミティブは **スロット型** を持ち、スロット型によって表現辞書に保存する日本語の「形」が決まる。

### 1.1 スロット型の定義

| スロット型 | 用途 | 保存する形 | 例 |
| --- | --- | --- | --- |
| `ADNOMINAL`（連体修飾フラグメント） | filter述語。名詞（「要素」「値」等）の直前に置ける形で保存し、複数個を単純連結できるようにする | 「〜の」「〜い」で終わる連体形1種類のみ | `"偶数の"`, `"k以上の"`, `"kより大きい"` |
| `ACTION_PAIR`（動作節ペア） | map・order・slice。動作を表す節を、**終止形**（文末で使う）と**連用中止形**（後ろに別の節が続く場合に使う）の**両方**を保存する | `{"terminal": "...する", "te": "...して"}` のペア | `{"terminal": "kを加える", "te": "kを加えて"}` |

`ADNOMINAL`をペアではなく単一形にしているのは、filterは常に「〜の要素を残す／抽出する」という**カテゴリ共通の枠**（後述2.1）に差し込むだけで、フラグメント自体が文末に来ることも他の節に続くこともないため。`ACTION_PAIR`を終止形/連用中止形のペアにしているのは、map/order/sliceは後ろに別のカテゴリがすぐ続く（連用中止形が要る）か、意味ASTの最後のアクティブカテゴリとして文を締める（終止形が要る）かが意味ASTごとに変わるため。

自動活用（「加える」→「加えて」等の機械的な連用中止形変換）はしない。Qwen3にte/terminalの両方を生成させ、人間が両方を承認する。理由: 活用ルールの自作はバグ源になりやすく、承認フローに乗せる方が「意味が変わっていないか」のチェックと一体化できる。

### 1.2 filter（抽出）— `ADNOMINAL` ×10

`schema.py`の`FILTER_GROUPS`をそのまま列挙する。同じ排他グループのものは意味ASTの中で同時に選ばれないが、辞書自体は10種すべて独立に持つ。

| op名 | グループ | 引数 | 表現辞書キー | 連体形の例（Qwen3に生成させる範囲） |
| --- | --- | --- | --- | --- |
| `even` | parity | なし | `filter:even` | 偶数の / 2で割り切れる |
| `odd` | parity | なし | `filter:odd` | 奇数の / 2で割り切れない |
| `gt_k` | k_compare | k | `filter:gt_k` | kより大きい / kを上回る |
| `ge_k` | k_compare | k | `filter:ge_k` | k以上の |
| `lt_k` | k_compare | k | `filter:lt_k` | kより小さい / kを下回る |
| `le_k` | k_compare | k | `filter:le_k` | k以下の |
| `multiple_of_k` | k_multiple | k | `filter:multiple_of_k` | kの倍数の / kで割り切れる |
| `positive` | sign | なし | `filter:positive` | 正の / 0より大きい |
| `negative` | sign | なし | `filter:negative` | 負の / 0より小さい |
| `zero` | sign | なし | `filter:zero` | ゼロの / 値が0の |

`k`を含む表現は、テンプレート中の`k`をリテラルトークン（プレースホルダ）として保持し、最終レンダリング時に実際の変数名`k`へ置換する（意味ASTの引数は常に実行時の`k`そのものであり、定数ではないため）。

### 1.3 map（変換）— `ACTION_PAIR` ×7 + `mul_const`

| op名 | 引数 | 表現辞書キー | terminal例 | te例 |
| --- | --- | --- | --- | --- |
| `add_k` | なし（kを暗黙参照） | `map:add_k` | kを加える | kを加えて |
| `sub_k` | なし | `map:sub_k` | kを引く | kを引いて |
| `mul_k` | なし | `map:mul_k` | kを掛ける | kを掛けて |
| `negate` | なし | `map:negate` | 符号を反転する | 符号を反転して |
| `abs` | なし | `map:abs` | 絶対値を取る | 絶対値を取って |
| `square` | なし | `map:square` | 二乗する | 二乗して |
| `mul_const` | 定数N (2または3) | `map:mul_const` | N倍する | N倍して |

`mul_const`は引数によって表現が変わらない（「2倍する」「3倍する」は同じテンプレート`"N倍する"`のNだけが変わる）ため、辞書エントリは1つで、`N`もプレースホルダとして保持し定数`2`または`3`で置換する。`add_k`等の「なし」は暗黙に`k`という変数名を文中に埋め込む（プレースホルダ`k`は1.2と共通）。

### 1.4 order（並べ替え）— `ACTION_PAIR` ×3（単一選択）

| op名 | 表現辞書キー | terminal例 | te例 |
| --- | --- | --- | --- |
| `ascending` | `order:ascending` | 昇順に並べる | 昇順に並べて |
| `descending` | `order:descending` | 降順に並べる | 降順に並べて |
| `reverse` | `order:reverse` | 順序を逆にする | 順序を逆にして |

同一カテゴリ内チェイン（1.5参照）は`order`には適用しない（`schema.py`が単一選択に制限している設計判断と一致させる）。

### 1.5 slice（切り出し）— `ACTION_PAIR` ×3

| op名 | 引数 | 表現辞書キー | terminal例 | te例 |
| --- | --- | --- | --- | --- |
| `take_first_k` | なし（k参照） | `slice:take_first_k` | 先頭からk個を取り出す | 先頭からk個を取り出して |
| `take_last_k` | なし | `slice:take_last_k` | 末尾からk個を取り出す | 末尾からk個を取り出して |
| `step_2` | なし | `slice:step_2` | 1個おきに取り出す | 1個おきに取り出して |

### 1.6 文頭・文末フレーム（プリミティブではないが同じ枠組みで表現辞書化する）

これらも「原子操作」ではないが、Qwen3に10〜数種類生成させ人間承認するという同じフローに乗せることで、テンプレートの単調さ（同じ書き出し・結びの繰り返し）を防ぐ。

| 表現辞書キー | 役割 | 例 |
| --- | --- | --- |
| `frame:opening` | 文頭。xsという入力を導入する | 「整数リストxsから、」「xsという整数のリストに対して、」「与えられたリストxsについて、」 |
| `frame:closing` | 文末。最後のアクティブカテゴリの終止形節を受けて依頼文にする | 「〜solve関数を書いてください。」「〜Python関数solveを実装してください。」「〜処理を行うsolveという関数を作成してください。」 |

---

## 2. 組み合わせ型（テンプレート合成規則）

意味AST 1件のレンダリングは、`filter → map → order → slice` という`schema.py`のパイプライン順を**そのまま日本語の節の並び順**として使う（これは`schema.py`のdocstring「抽出してから変換して並べ替えて切り出す」という設計と一致）。

### 2.1 filterカテゴリの結合規則

`filters`は0〜2個の`ADNOMINAL`フラグメントの集合（順不同・AND結合）。

1. 各フィルタopの承認済み`ADNOMINAL`表現から1つを抽選し、そのまま**単純連結**する（連体形どうしは日本語として連結可能: 「k以上の」+「偶数の」→「k以上の偶数の」）。
2. 連結したフラグメント列に、**カテゴリ共通の枠テンプレート**（`frame:filter_verb`、これも承認済み表現辞書、5〜10種）を適用する。枠テンプレートは「{frag}要素だけを残し」「{frag}ものを抽出し」「{frag}値だけを選び」のような形で、`{frag}`部分に連結済みフラグメントを差し込む。
3. filterが0個（このASTでカテゴリが非アクティブ）の場合、このカテゴリの節ごと出力しない。

出力される節は「〜要素だけを残し」のような**連用中止形**で終わる（枠テンプレート自体がすでに連用中止形で終わるよう承認時に統一する）。filterが最後のアクティブカテゴリになるケースは存在しない設計にする（後述2.3の通り、filterの後に他のカテゴリがない場合は枠テンプレートの終止形バリアント「〜要素だけを残す」を使う）。

### 2.2 map / slice カテゴリの結合規則（チェイン）

`map_ops`・`slice_ops`はそれぞれ0〜2個の`ACTION_PAIR`の**順序付き**列。

1. 1個目の要素だけの場合: そのカテゴリがパイプライン内で最後のアクティブカテゴリなら`terminal`形を、そうでなければ`te`形を使う。
2. 2個目まである場合（チェイン）: 1個目は常に`te`形を使い、接続詞として「から」を補う（`schema.py`のREADME例「kを加えてから2倍する」に合わせ、`te形+から`を標準の連結子にする）。2個目は、そのカテゴリがパイプライン内で最後のアクティブカテゴリなら`terminal`形、そうでなければ`te`形。
3. 順序は`map_ops`/`slice_ops`のタプル順をそのまま日本語の出現順に対応させる（`schema.py`が「順序は結果に影響する」と明記しているため、日本語表現でも順序を保存する）。

### 2.3 order カテゴリの結合規則

`order_op`は0または1個。`ACTION_PAIR`から、最後のアクティブカテゴリなら`terminal`、そうでなければ`te`を使うだけ（チェインは発生しない）。

### 2.4 カテゴリ間の結合（文全体の組み立て）

1. アクティブなカテゴリ（`filter`/`map`/`order`/`slice`のうち意味ASTで使われているもの、1〜3個）を`schema.py`のパイプライン順に並べる。
2. 最後のカテゴリ以外はすべて「非最終形」（filterなら枠テンプレートの連用中止形、map/order/sliceなら`te`形）でレンダリングし、単純に連結する（読点はプリミティブ表現側に含める設計にし、生成器側では追加しない、または生成器側で「、」を一律追加する、のどちらかに統一する——実装時に決定）。
3. 最後のカテゴリは「終止形」（filterなら枠テンプレートの終止バリアント、map/order/sliceなら`terminal`形）でレンダリングする。
4. `frame:opening`を先頭に、`frame:closing`を末尾に付与する。`frame:closing`は「終止形節＋closing」の形で自然に繋がるよう、closing表現は動詞の連体形接続（「〜solve関数を書いてください」なら直前は「昇順に並べる」のような連体修飾可能な終止形）を前提に承認する。

### 2.5 プレースホルダ置換

レンダリング後の文字列には`k`と（`mul_const`使用時のみ）`N`がプレースホルダとして残る。`k`はそのままの文字列「k」で確定（意味AST自体がkを変数として参照するため置換不要）。`N`は`mul_const`の実引数（2または3）で置換する。

### 2.6 具体例

意味AST:
```json
{"filter": ["ge_k", "even"], "map": [["add_k"], ["mul_const", 2]], "order": "ascending"}
```

1. filter: `["k以上の", "偶数の"]` → 連結「k以上の偶数の」→ 枠テンプレート（連用中止）「k以上の偶数の要素だけを残し、」
2. map: chain, 最後のカテゴリではない（orderが後に続く）ので両方te形: 「kを加えてから2倍して、」
3. order: 最後のアクティブカテゴリなのでterminal: 「昇順に並べる」
4. 組み立て: `frame:opening`「整数リストxsから、」+ 1 + 2 + 3 + `frame:closing`「〜solve関数を書いてください。」

→「整数リストxsから、k以上の偶数の要素だけを残し、kを加えてから2倍して、昇順に並べるsolve関数を書いてください。」

---

## 3. 承認済み表現辞書のデータ構造（案）

```json
{
  "filter:ge_k": {
    "slot_type": "ADNOMINAL",
    "expressions": ["k以上の", "k以上である"]
  },
  "map:add_k": {
    "slot_type": "ACTION_PAIR",
    "expressions": [
      {"terminal": "kを加える", "te": "kを加えて"},
      {"terminal": "kを足す", "te": "kを足して"}
    ]
  },
  "frame:filter_verb": {
    "slot_type": "ACTION_PAIR",
    "expressions": [
      {"terminal": "{frag}要素だけを残す", "te": "{frag}要素だけを残し"},
      {"terminal": "{frag}ものを抽出する", "te": "{frag}ものを抽出し"}
    ]
  },
  "frame:opening": {"slot_type": "TEXT", "expressions": ["整数リストxsから、", "xsという整数のリストに対して、"]},
  "frame:closing": {"slot_type": "TEXT", "expressions": ["solve関数を書いてください。", "Python関数solveを実装してください。"]}
}
```

このJSONは`semantic_ast/`と同様、プリミティブ・キー（op_tagと1対1）を主キーにしており、`schema.py`の`op_tags()`と同じ命名（`filter:even`, `map:mul_const`等）を使うことで、生成器コードとの対応を機械的に取れるようにする。

同じ意味ASTに対し、表現辞書からの抽選バリエーション（フィルタの複数表現、枠テンプレートの複数バリエーション、opening/closingの複数バリエーション）の組み合わせだけで数十〜数百通りの異なる`instruction_ja`を機械的に作れるため、「言い換えテスト」（`homework.md`の訓練に使用しなかった日本語テンプレートを使うテスト）用に、特定の表現辞書エントリやframeバリエーションをtrain側では使わずval/test専用に予約しておく、という分割もこの表現辞書の粒度でできる。

---

## 4. Qwen3への問い合わせ設計

- 問い合わせ単位は**プリミティブ1つ**（表現辞書のキー1つ）。総数は filter 10 + map 7 + mul_const 1 + order 3 + slice 3 + frame:filter_verb 1 + frame:opening 1 + frame:closing 1 = **27プロンプト**程度。40,589件ではなくこの規模なので、人間チェックが現実的な件数（27 × 10〜30 ≒ 300〜800件）に収まる。
- `ACTION_PAIR`が必要なプリミティブは、1回のプロンプトで「終止形と連用中止形を両方、対応づけて10〜30ペア」生成させる（terminalとteがズレると組み合わせ時に非文法的な文になるため、ペアで生成・ペアで承認する）。
- `homework.md`が明記する記録項目（モデル名・revision・量子化方式・推論ライブラリversion・system prompt・sampling設定・seed・生成日時・プロンプトのハッシュ値）をプリミティブ単位の生成ログにも同様に残す。
- 人間チェックの観点は`homework.md`が指摘する通り「以上→より大きい」のような意味を変える言い換えの混入を主に見る。プリミティブ単位なので、1つのop_tagにつき候補が10〜30件と少なく、チェックの負荷は意味AST単位（40,589件）でチェックする場合と比べて数百分の一になる。

## 5. 未確定事項（実装時に決める）

- 読点「、」を表現辞書側の文字列に含めるか、生成器側で一律付与するか。
- `frame:filter_verb`の`{frag}`が0個（filterカテゴリ自体が非アクティブ）の場合はこの枠自体を使わないので問題ないが、filterが1個だけの場合に不自然にならない表現セットになっているか（人間チェック時に確認）。
- `positive`/`negative`/`zero`のADNOMINAL表現は「ゼロの」等やや不自然になりやすいため、承認段階で候補を絞る。
- 表現辞書ファイルの物理配置（`semantic_ast/expressions/*.json`を想定）と、生成器モジュール（`semantic_ast/ja_generator.py`を想定、本計画のレンダラを実装する）の実装は別タスク。
